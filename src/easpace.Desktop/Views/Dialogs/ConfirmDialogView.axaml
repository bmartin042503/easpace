<!--
Copyright (c) 2026 Martin Bartos
Licensed under the MIT License. See LICENSE file for details.
-->

<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:easpace.Desktop.ViewModels.Dialogs"
             x:DataType="vm:ConfirmDialogViewModel"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="250"
             MaxWidth="550" MaxHeight="400"
             Background="Transparent"
             x:Class="easpace.Desktop.Views.Dialogs.ConfirmDialogView">

    <Border x:Name="DialogRootBorder" Classes="dialog card"
            Padding="{StaticResource Metrics.Spacing.Thickness.Sm}"
            BorderThickness="0">

        <Grid RowDefinitions="Auto,*,Auto"
              RowSpacing="{StaticResource Metrics.Spacing.Lg}">

            <Grid Grid.Row="0" ColumnDefinitions="Auto,*"
                  ColumnSpacing="{StaticResource Metrics.Spacing.Sm}">

                <!-- Warning icon -->
                <PathIcon Grid.Column="0" Height="{StaticResource Metrics.Icon.Size.Lg}"
                          Width="{StaticResource Metrics.Icon.Size.Lg}"
                          Foreground="{DynamicResource Sem.Foreground}"
                          Data="{StaticResource PhosphorIcons.Warning.Fill}"
                          IsVisible="{Binding IsDestructive}"
                          VerticalAlignment="Top"
                          Margin="0,4,0,0" />

                <!-- Info icon -->
                <PathIcon Grid.Column="0" Height="{StaticResource Metrics.Icon.Size.Lg}"
                          Width="{StaticResource Metrics.Icon.Size.Lg}"
                          Foreground="{DynamicResource Sem.Foreground}"
                          Data="{StaticResource PhosphorIcons.Info.Fill}"
                          IsVisible="{Binding !IsDestructive}"
                          VerticalAlignment="Top"
                          Margin="0,4,0,0" />

                <!-- Title -->
                <TextBlock x:Name="DialogRootTitle" Grid.Column="1"
                           Classes="neutral fs3 trm-ellipsis" Text="{Binding Title}"
                           TextWrapping="Wrap" VerticalAlignment="Center" />

            </Grid>

            <!-- Message -->
            <TextBlock Grid.Row="1" Classes="neutral fs4 trm-ellipsis"
                       Text="{Binding Message}"
                       VerticalAlignment="Center" HorizontalAlignment="Left"
                       TextWrapping="Wrap" TextAlignment="Left" />

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="{StaticResource Metrics.Spacing.Xs}"
                        HorizontalAlignment="Right">

                <!-- Cancel button -->
                <Button Classes="secondary regular" Content="{Binding CancelText}"
                        Command="{Binding CancelCommand}" />

                <!-- Confirm button -->
                <Button Classes.danger="{Binding IsDestructive}"
                        Classes.primary="{Binding !IsDestructive}"
                        Classes="regular" Content="{Binding ConfirmText}"
                        Command="{Binding ConfirmCommand}" />

            </StackPanel>

        </Grid>

    </Border>

</UserControl>